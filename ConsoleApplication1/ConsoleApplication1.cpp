#include <iostream>
#include <Windows.h>
#include <conio.h>
#include <ctime>
#include "GlobalSet.h"
#include "Prototype.h"



using namespace std;








int main()
{
    int speed = 100;
    srand(time(NULL));
    sizeWindow(WIDTH, HIGHT, TOP, LEFT);
    menu(speed, xb, yb);





}